#ifndef LIFECYCLE_HPP
#define LIFECYCLE_HPP

/**
 * @brief Initialize the game to the default state
 */
void InitializeGame();
/**
 * @brief Perform actions for player death
 */
void GameOver();
/**
 * @brief Reset the game to the initial state
 */
void ResetGame();
#endif // LIFECYCLE_HPP
