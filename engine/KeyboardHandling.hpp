#ifndef KEYBOARDHANDLING_HPP
#define KEYBOARDHANDLING_HPP

#include <SDL3/SDL_events.h>
/**
 * @brief Handle a keyboard event
 *
 * @param e The keyboard event to handle
 */
void HandleKeyboardEvent(SDL_Event& e);

#endif // KEYBOARDHANDLING_HPP
